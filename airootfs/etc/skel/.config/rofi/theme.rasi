/* Night sky rofi theme — colours matched to MangoWM */

* {
    bg:         #1b3555ff;
    bg-alt:     #2c4a6dff;
    fg:         #d4e4f0ff;
    fg-dim:     #7089a5ff;
    accent:     #8ea1bbff;
    urgent:     #c4607aff;
    selected-fg:#1b3555ff;

    background-color:   transparent;
    text-color:         @fg;
    border-color:       @accent;
}

window {
    background-color:   @bg;
    border:             4px;
    border-color:       @accent;
    border-radius:      6px;
    width:              30%;
    padding:            12px;
}

mainbox {
    background-color:   transparent;
    children:           [ inputbar, listview ];
    spacing:            8px;
}

inputbar {
    background-color:   @bg-alt;
    border-radius:      4px;
    padding:            8px 10px;
    children:           [ prompt, entry ];
    spacing:            6px;
}

prompt {
    text-color:         @accent;
}

entry {
    text-color:         @fg;
    placeholder:        "Search...";
    placeholder-color:  @fg-dim;
}

listview {
    background-color:   transparent;
    lines:              8;
    scrollbar:          false;
    spacing:            2px;
}

element {
    background-color:   transparent;
    border-radius:      4px;
    padding:            6px 10px;
    spacing:            8px;
    children:           [ element-icon, element-text ];
}

element selected.normal {
    background-color:   @accent;
    text-color:         @selected-fg;
}

element urgent {
    text-color:         @urgent;
}

element-icon {
    size:               1.2em;
}

element-text {
    text-color:         inherit;
    vertical-align:     0.5;
}
